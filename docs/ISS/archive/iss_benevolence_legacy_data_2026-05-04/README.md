# 旧互換データ

このディレクトリは、過去のclosed-loop実装を参照するための退避場所です。
ISS正本データの主線ではなく、引き渡し対象にも含めません。

現在の主線は `../data/agents.tsv`、`../data/events_run_a.tsv`、`../data/events_run_b.tsv`、`../data/places_iss_10.tsv`、`../data/objects_menu.tsv` です。
